# auto-generated by grc.converter

id: variable_frame_sync_config_concatenated
label: Frame Sync. Config (Concatenated)
category: '[ccsds]'
flags:
- show_id

parameters:
-   id: modulation
    label: Modulation
    dtype: enum
    options: [bpsk, qpsk]
    option_labels: [BPSK, QPSK]
-   id: interleaving_depth
    label: Interleaving depth
    dtype: int
    default: '4'
value: ${ ccsds.frame_sync_config(ccsds.constellation_ccsds_modulation.base(),'81C971AA73D3E0',52,interleaving_depth*255*8*2+12,0,0)
    }

templates:
    imports: import ccsds
    var_make: "\nself.${id} = ${id} = ccsds.frame_sync_config(ccsds.constellation_ccsds_${modulation}().base(),\n\
        \    '81C971AA73D3E0',\n    52,\n    ${interleaving_depth}*255*8*2+12,\n \
        \   0,\n    0\n)\n\t"

file_format: 1
