# auto-generated by grc.converter

id: ccsds_mpsk_mod_bc
label: M-PSK Modulator (CCSDS)
category: CCSDS/Sender

parameters:
-   id: M
    label: M
    dtype: int
    default: '8'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
asserts:
- ${ M >= 2 }

templates:
    imports: import ccsds
    make: ccsds.mpsk_mod_bc(${M})

documentation: |-
    M-PSK symbol modulator.

    Take a byte with a value between 0 and M-1 and transform it into a complex M-PSK symbol.

    M: Number of modulation symbols.

file_format: 1
