# auto-generated by grc.converter

id: ccsds_softbit_msg_source_f
label: Softbit Message Source
category: CCSDS/Conversion

parameters:
-   id: frame_len
    label: Frame length
    dtype: int
    default: '40'

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.softbit_msg_source_f(${frame_len})

documentation: |-
    Softbit message source

    Copies the floats contained in the incomming Softbit messages to the output stream.

    Frame length: Length of a frame in (soft)bits.

file_format: 1
