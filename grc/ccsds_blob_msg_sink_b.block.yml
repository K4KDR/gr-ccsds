# auto-generated by grc.converter

id: ccsds_blob_msg_sink_b
label: BLOB Message Sink
category: CCSDS/Conversion

parameters:
-   id: blob_len
    label: BLOB length
    dtype: int
    default: '40'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out

templates:
    imports: import ccsds
    make: ccsds.blob_msg_sink_b(${blob_len})

documentation: |-
    BLOB Message sink

    Copies portions of BLOB length bytes from the input stream to BLOB messages.

    BLOB length: Length of a BLOB in bytes.

file_format: 1
