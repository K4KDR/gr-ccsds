# auto-generated by grc.converter

id: ccsds_softbit_msg_sink_f
label: Softbit Message Sink
category: CCSDS/Conversion

parameters:
-   id: frame_len
    label: Frame length
    dtype: int
    default: '40'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: message
    id: out

templates:
    imports: import ccsds
    make: ccsds.softbit_msg_sink_f(${frame_len})

documentation: |-
    Softbit message sink

    Copies portions of Frame length softbits from the input stream to a softbit message.

    Frame length: Length of a frame in (soft)bits.

file_format: 1
