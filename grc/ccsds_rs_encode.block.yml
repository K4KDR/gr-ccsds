# auto-generated by grc.converter

id: ccsds_rs_encode
label: Reed Solomon Encoder (CCSDS)
category: CCSDS/Channelcoding

parameters:
-   id: I
    label: Length of interleaving
    dtype: enum
    default: '5'
    options: ['1', '2', '3', '4', '5', '8']
    option_attributes:
        fcn: ['1', '2', '3', '4', '5', '8']

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.rs_encode(int(${I.fcn}))

documentation: |-
    Reed Solomon Encoder
    Frame interleaving and RS encoding according to CCSDS (32 byte parity)

    Length of interleaving: I as specified in ECSS. Can be 1, 2, 3, 4, 5, or 8
    Incomming messages must be of size 223*I, outgoing messages of size 255*I bytes.

file_format: 1
