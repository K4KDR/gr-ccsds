# auto-generated by grc.converter

id: ccsds_sort_frames
label: sort_frames
category: '[ccsds]'

parameters:
-   id: window_size
    label: Window size
    dtype: int
    default: '10'
-   id: allow_duplicates
    label: Allow duplicates
    dtype: bool
    default: 'False'
-   id: allow_duplicates
    label: Allow duplicates
    dtype: bool
    default: 'False'
-   id: sort_field
    label: Sort field
    dtype: str
    default: '''asm_number'''
-   id: score_field
    label: Score field
    dtype: str
    default: '''asm_score'''
-   id: pick_highest_score
    label: Pick highest score
    dtype: bool
    default: 'True'
-   id: mode
    label: Output Mode
    dtype: enum
    default: ccsds.SORT_FRAME_OUTPUT_QUIET
    options: [ccsds.ccsds.SORT_FRAME_OUTPUT_QUIET, ccsds.ccsds.SORT_FRAME_OUTPUT_DROPPED,
        ccsds.ccsds.SORT_FRAME_OUTPUT_DUPLICATE, ccsds.ccsds.SORT_FRAME_OUTPUT_ALL]
    option_labels: [Quiet, Dropped Frames, Duplicate Frames, All Frames]
    option_attributes:
        fcn: [ccsds.ccsds.SORT_FRAME_OUTPUT_QUIET, ccsds.ccsds.SORT_FRAME_OUTPUT_DROPPED,
            ccsds.ccsds.SORT_FRAME_OUTPUT_DUPLICATE, ccsds.ccsds.SORT_FRAME_OUTPUT_ALL]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.sort_frames(${window_size}, ${allow_duplicates}, ${sort_field}, ${score_field},
        ${pick_highest_score})

file_format: 1
