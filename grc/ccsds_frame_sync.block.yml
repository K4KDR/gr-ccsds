# auto-generated by grc.converter

id: ccsds_frame_sync
label: Frame Synchronization (symb)
category: '[ccsds]'

parameters:
-   id: sync_config
    label: Frame synchronization config
    dtype: raw
-   id: gen_config
    label: Frame generation config
    dtype: raw
-   id: additional_frames
    label: Additional ASM positions
    dtype: raw
    default: ccsds.asm_position_list()

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: message
    id: out

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.frame_sync(${sync_config}, ${gen_config}, ${additional_frames})

file_format: 1
