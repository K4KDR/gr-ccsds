# auto-generated by grc.converter

id: variable_frame_sync_config
label: Frame Sync. Config
category: '[ccsds]'
flags:
- show_id

parameters:
-   id: constellation
    label: Constellation
    dtype: raw
-   id: sync_marker
    label: Sync Marker (hex)
    dtype: string
    default: '''1ACFFC1D'''
-   id: sync_num_bits
    label: Sync marker length (bits)
    dtype: int
    default: '32'
-   id: payload_len_bits
    label: Payload length (bits)
    dtype: int
    default: 4*255*8
-   id: num_head_bits
    label: 'Copy # of bits before frame'
    dtype: int
    default: '0'
-   id: num_tail_bits
    label: 'Copy # of bits after frame'
    dtype: int
    default: '0'
value: ${ value }

templates:
    imports: import ccsds
    var_make: self.${id} = ${id} = ccsds.frame_sync_config(${constellation}, ${sync_marker},
        ${sync_num_bits}, ${payload_len_bits}, ${num_head_bits}, ${num_tail_bits})

file_format: 1
