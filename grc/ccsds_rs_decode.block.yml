# auto-generated by grc.converter

id: ccsds_rs_decode
label: Reed Solomon Decoder (CCSDS)
category: CCSDS/Channelcoding

parameters:
-   id: I
    label: Length of interleaving
    dtype: enum
    default: '5'
    options: ['1', '2', '3', '4', '5', '8']
    option_attributes:
        fcn: ['1', '2', '3', '4', '5', '8']
-   id: repr
    label: Data representation
    dtype: enum
    default: '0'
    options: ['0', '1']
    option_labels: [Berlekamp, Conventional]
    option_attributes:
        fcn: ['0', '1']
-   id: verbosity
    label: Output
    dtype: enum
    default: '0'
    options: [ccsds.DECODER_VERBOSITY_QUIET, ccsds.DECODER_VERBOSITY_DROPPED, ccsds.DECODER_VERBOSITY_ALL,
        ccsds.DECODER_VERBOSITY_DEBUG]
    option_labels: [Quiet, Dropped frames, All frames, Debug]
    option_attributes:
        fcn: [ccsds.DECODER_VERBOSITY_QUIET, ccsds.DECODER_VERBOSITY_DROPPED, ccsds.DECODER_VERBOSITY_ALL,
            ccsds.DECODER_VERBOSITY_DEBUG]

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import ccsds
    make: ccsds.rs_decode(int(${I.fcn}),int(${repr.fcn}),int(${verbosity.fcn}))

documentation: |-
    Reed Solomon Decoder
    Frame interleaving and RS decoding according to CCSDS (32 byte parity)

    Length of interleaving: I as specified in ECSS. Can be 1, 2, 3, 4, 5, or 8
    Incomming messages must be of size 255*I, outgoing messages of size 223*I

    Data representation: Which data representation should be used. CCSDS
    specifies the Berlekamp (or dual basis) representation. Conventional
    representation is often used in literature and is also used in Matlab
    but is _not_ compatible with CCSDS.

file_format: 1
