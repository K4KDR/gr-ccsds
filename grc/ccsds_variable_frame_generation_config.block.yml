# auto-generated by grc.converter

id: variable_frame_generation_config
label: Frame Generation Config
category: '[ccsds]'
flags:
- show_id

parameters:
-   id: numbefore
    label: Num before
    dtype: int
    default: '0'
-   id: numafter
    label: Num after
    dtype: int
    default: '0'
-   id: thresholdprimary
    label: Primary threshold
    dtype: real
    default: '0.0'
-   id: thresholdsecondary
    label: Secondary threshold
    dtype: real
    default: '0.0'
value: ${ value }

templates:
    imports: import gnuradio.ccsds as ccsds
    var_make: self.${id} = ${id} = ccsds.frame_generation_config(${numbefore}, ${numafter}, ${thresholdprimary}, ${thresholdsecondary})

file_format: 1
