# auto-generated by grc.converter

id: ccsds_msg_null_src
label: Message null source (CCSDS)
category: CCSDS/Misc

outputs:
-   domain: message
    id: out

templates:
    imports: import ccsds
    make: ccsds.msg_null_src()

documentation: |-
    Defines a message out port that is never used. This block can be used to connect
    asynchronous blocks to the flowgraph and then pass in messages to them directly
    from the python code. This block should only be used in QA code.

file_format: 1
