# auto-generated by grc.converter

id: ccsds_softbits_msg_to_bytes_b
label: Softbits message to bytes (CCSDS)
category: CCSDS/Conversion

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.softbits_msg_to_bytes_b()

documentation: |-
    Softbit message to byte converter.

    Receives messages of softbits and converts them into packed bytes (with hard
    decisions).

file_format: 1
