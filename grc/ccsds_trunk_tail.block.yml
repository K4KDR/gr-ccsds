# auto-generated by grc.converter

id: ccsds_trunk_tail
label: Trunk Tail (CCSDS)
category: CCSDS/Misc

parameters:
-   id: tail_len
    label: Number of bits to trunkate
    dtype: int
    default: '''92'''
-   id: frame_len
    label: Frame data length
    dtype: int
-   id: blanc_bits
    label: uncertain bits at the end
    dtype: int
    default: '12'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
asserts:
- ${ tail_len >= 0 }
- ${ frame_len > 0 }
- ${ blanc_bits >= 0 }

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.trunk_tail(${tail_len},${frame_len}, ${blanc_bits})

documentation: |-
    Add ASM
    Take a raw frame as input and output the ASM followed by the raw frame data.

    Sync Marker: Hexadecimal representation of the ASM. Must be of odd length as the
    ASM must be an integer number of bytes.

    Frame data length: Length of the frame data (excluding the ASM) in bytes.

file_format: 1
